version: '3.3'
services:
 web_chat:
   build: .
   command: python manage.py runserver 0.0.0.0:8000
   volumes:
     - .:/code
   ports:
     - "8000:8000"
   depends_on:
     - chat_db
 chat_db:
   image: postgres